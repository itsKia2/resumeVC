{
    "name": "resumevc-server",
    "version": "0.0.0",
    "description": "ResumeVC Flask server",
    "private": true,
    "scripts": {
        "dev": "uv run flask --app app run --debug",
        "build": "uv sync --no-dev",
        "clean": "uv clean && rm -rf .venv __pycache__"
    }
}